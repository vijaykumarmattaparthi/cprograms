#include <stdio.h>
int main()
{
	int a, b, rem ;
	printf ("Enter value of Divisor: ");
	scanf ("%d",&a);
	printf ("Enter value of Dividend: ");
	scanf ("%d",&b);
	rem = a % b;
	printf ("Remainder = %d",rem);
	quotient `
	
	return 0;
}

