#include <stdio.h>
int main()
{
	int a,b,c;
	printf ("Enter a value of A: ");
	scanf ("%d",&a);
	printf ("Enter a value of B: ");
        scanf ("%d",&b);
	printf ("Enter a value of C: ");
        scanf ("%d",&c);
	if(a>b,a>c)
		printf ("%d",a);
	if(b>a,b>c)
		printf ("%d",b);
	if(c>a,c>b)
		printf ("%d",c);
	return 0;
}
